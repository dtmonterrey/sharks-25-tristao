const canvas = document.getElementById('canvas')
const ctx = canvas.getContext('2d')
const width = 640
const height = 480

export default {
    canvas: canvas,
    ctx: ctx, 
    width: width,
    height: height,
}
